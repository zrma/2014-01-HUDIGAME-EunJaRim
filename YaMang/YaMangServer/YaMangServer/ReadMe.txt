# 피드백

1. 데이터시트에서 읽어와서.. 섭따까지 변하지 않는 UnitData같은거는

class DatasheetMananger 하나 전역으로 만들어 놓고..
{
   bool LoadDatasheet();
    ... ...
}

GDatasheetMananger->GetUnitData(UT_ARROW).mHP  이런 식으로 참조..

2. UsedAction* 같은거 쓰는거는 좋은 방법이 아니다. 객체의 생애주기는 확실하게 처리하는 습관을 들이도록.

3. 네트워크 등의 하위 로직과 게임 콘텐츠 등의 상위 로직을 분리하는 리팩토링 연습을 해볼 것... (이번 학기 끝나고?)

4. C++ STL 자료구조들은 그 내부 특성을 잘 알고 항상 심사 숙고해서 사용해야 함. 다른 자바나 C# 처럼 컬렉션 객체들 마구 쓰듯이 사용하면 안됨..



# 나머지는 ///# 검색 ㄱㄱ